import { escapeHtml } from "../app/html"
import { getApp, goBack } from "../nav"
import { addHomesteadRecipe, addHomesteadTutorial, getHomesteadRecipes, getHomesteadTutorials, recipeCategorySuggestions, removeHomesteadRecipe, removeHomesteadTutorial } from "../features/homestead-library"
import "./homestead-lab.css"

export function renderHomesteadLab(category = "All") {
  const recipes = getHomesteadRecipes()
  const tutorials = getHomesteadTutorials()
  const categories = ["All", ...new Set([...recipeCategorySuggestions, ...recipes.map(recipe => recipe.category)])]
  const visibleRecipes = category === "All" ? recipes : recipes.filter(recipe => recipe.category === category)
  const categoryButtons = categories.map(item => '<button type="button" data-recipe-category="' + escapeHtml(item) + '"' + (item === category ? ' aria-pressed="true"' : "") + ">" + escapeHtml(item) + "</button>").join("")
  const recipeCards = visibleRecipes.length
    ? visibleRecipes.map(recipe => '<article class="recipe-box-card"><header><span>' + escapeHtml(recipe.category) + '</span><button class="recipe-remove" data-remove-recipe="' + escapeHtml(recipe.id) + '" aria-label="Remove ' + escapeHtml(recipe.title) + '">×</button></header><h3>' + escapeHtml(recipe.title) + '</h3>' + (recipe.notes ? '<p>' + escapeHtml(recipe.notes) + '</p>' : "") + '<details><summary>Open recipe</summary>' + (recipe.ingredients.length ? '<h4>Ingredients</h4><ul>' + recipe.ingredients.map(item => "<li>" + escapeHtml(item) + "</li>").join("") + "</ul>" : "") + '<h4>Directions</h4><ol>' + recipe.steps.map(item => "<li>" + escapeHtml(item) + "</li>").join("") + "</ol></details></article>").join("")
    : '<article class="recipe-box-empty"><span>✦</span><h3>Your ' + escapeHtml(category === "All" ? "family recipe box" : category) + ' collection is ready.</h3><p>Save a favorite food, a face mask, a craft formula, or any family process you want to keep.</p></article>'
  const tutorialCards = tutorials.length
    ? tutorials.map(tutorial => '<article class="how-to-card"><header><span>' + escapeHtml(tutorial.category) + '</span><button class="recipe-remove" data-remove-tutorial="' + escapeHtml(tutorial.id) + '" aria-label="Remove ' + escapeHtml(tutorial.title) + '">×</button></header><h3>' + escapeHtml(tutorial.title) + '</h3>' + (tutorial.notes ? '<p>' + escapeHtml(tutorial.notes) + '</p>' : "") + '<details><summary>Open tutorial</summary>' + (tutorial.materials.length ? '<h4>What you need</h4><ul>' + tutorial.materials.map(item => "<li>" + escapeHtml(item) + "</li>").join("") + "</ul>" : "") + '<h4>How to do it</h4><ol>' + tutorial.steps.map(item => "<li>" + escapeHtml(item) + "</li>").join("") + "</ol></details></article>").join("")
    : '<article class="how-to-empty"><span>🧵</span><h3>Save how your home does things.</h3><p>Capture tutorials for crafts, routines, repairs, gardening, family traditions, or any useful skill.</p></article>'

  getApp().innerHTML = '<main class="homestead-lab recipe-box-page"><header class="recipe-box-header"><button class="back-btn" id="lab-back">← Back</button><div><p class="os-kicker">FAMILY KNOW-HOW · SAVED LOCALLY</p><h1>Homestead Lab</h1><p>A beautiful home for the recipes, formulas, craft ideas, and step-by-step wisdom your family actually uses.</p></div><aside class="recipe-box-counts"><strong>' + recipes.length + '</strong><span>saved recipes</span><strong>' + tutorials.length + '</strong><span>how-tos</span></aside></header><section class="recipe-box-intro"><div><p class="os-kicker">THE FAMILY RECIPE BOX</p><h2>Keep every kind of recipe in one beautiful place.</h2><p>Food and baking, yes—but also skincare, crafts, garden mixes, cleaning formulas, and any category your home needs.</p></div><div class="recipe-box-tags"><span>🍲 Food</span><span>🧁 Baking</span><span>🧴 Skincare</span><span>🧵 Crafts</span><span>✦ Your categories</span></div></section><section class="recipe-category-bar"><div><strong>Browse the box</strong><div>' + categoryButtons + '</div></section><section class="recipe-workspace"><div class="recipe-collection"><div class="recipe-section-heading"><div><p class="os-kicker">SAVED RECIPES</p><h2>' + escapeHtml(category === "All" ? "Everything in the box" : category) + '</h2></div><small>Only this device can see these cards.</small></div><div class="recipe-box-grid">' + recipeCards + '</div></div><aside class="recipe-add-panel"><p class="os-kicker">ADD A RECIPE</p><h2>Write it down while it is fresh</h2><p>A recipe can be edible or not—just give it a category and clear steps.</p><form id="recipe-form"><label>Recipe name<input id="recipe-title" required maxlength="100" placeholder="For example: Sunday cinnamon rolls"></label><label>Category<input id="recipe-category" required list="recipe-category-list" maxlength="60" placeholder="Food, Skincare, Crafts…"></label><datalist id="recipe-category-list">' + recipeCategorySuggestions.map(item => "<option value=\"" + escapeHtml(item) + '\">').join("") + '</datalist><label>Ingredients or supplies <small>one per line</small><textarea id="recipe-ingredients" maxlength="1800" placeholder="2 cups flour&#10;1 teaspoon cinnamon"></textarea></label><label>Directions <small>one step per line</small><textarea id="recipe-steps" required maxlength="2400" placeholder="Mix the dry ingredients.&#10;Add the wet ingredients.&#10;Bake until done."></textarea></label><label>Notes <small>optional</small><textarea id="recipe-notes" maxlength="1200" placeholder="Allergies, substitutions, memories, or a helpful tip"></textarea></label><button class="os-primary">Save to recipe box</button><p class="parent-error" id="recipe-error"></p></form></aside></section><section class="how-to-shelf"><header><div><p class="os-kicker">THE HOW-TO SHELF</p><h2>Save tutorials your future self will thank you for.</h2><p>Store the exact steps for anything worth teaching, repeating, or remembering.</p></div><span>📚</span></header><div class="how-to-layout"><div class="how-to-grid">' + tutorialCards + '</div><aside class="tutorial-add-panel"><h3>Save a tutorial</h3><form id="tutorial-form"><label>Title<input id="tutorial-title" required maxlength="100" placeholder="For example: How we start seedlings"></label><label>Category<input id="tutorial-category" required maxlength="60" placeholder="Garden, Craft, Home…"></label><label>Materials <small>one per line</small><textarea id="tutorial-materials" maxlength="1800" placeholder="Seed tray&#10;Potting mix"></textarea></label><label>Steps <small>one per line</small><textarea id="tutorial-steps" required maxlength="2400" placeholder="Prepare the space.&#10;Work one step at a time."></textarea></label><label>Helpful notes <small>optional</small><textarea id="tutorial-notes" maxlength="1200" placeholder="Safety notes, variations, or a family tip"></textarea></label><button class="os-secondary">Save how-to</button><p class="parent-error" id="tutorial-error"></p></form></aside></div></section></main>'

  document.getElementById("lab-back")!.addEventListener("click", goBack)
  document.querySelectorAll<HTMLButtonElement>("[data-recipe-category]").forEach(button => button.addEventListener("click", () => renderHomesteadLab(button.dataset.recipeCategory)))
  document.getElementById("recipe-form")!.addEventListener("submit", event => {
    event.preventDefault()
    const saved = addHomesteadRecipe({ title: (document.getElementById("recipe-title") as HTMLInputElement).value, category: (document.getElementById("recipe-category") as HTMLInputElement).value, ingredients: (document.getElementById("recipe-ingredients") as HTMLTextAreaElement).value.split("\n"), steps: (document.getElementById("recipe-steps") as HTMLTextAreaElement).value.split("\n"), notes: (document.getElementById("recipe-notes") as HTMLTextAreaElement).value })
    if (saved) renderHomesteadLab(category)
    else document.getElementById("recipe-error")!.textContent = "Add a name, category, and at least one direction."
  })
  document.getElementById("tutorial-form")!.addEventListener("submit", event => {
    event.preventDefault()
    const saved = addHomesteadTutorial({ title: (document.getElementById("tutorial-title") as HTMLInputElement).value, category: (document.getElementById("tutorial-category") as HTMLInputElement).value, materials: (document.getElementById("tutorial-materials") as HTMLTextAreaElement).value.split("\n"), steps: (document.getElementById("tutorial-steps") as HTMLTextAreaElement).value.split("\n"), notes: (document.getElementById("tutorial-notes") as HTMLTextAreaElement).value })
    if (saved) renderHomesteadLab(category)
    else document.getElementById("tutorial-error")!.textContent = "Add a title, category, and at least one step."
  })
  document.querySelectorAll<HTMLButtonElement>("[data-remove-recipe]").forEach(button => button.addEventListener("click", () => { removeHomesteadRecipe(button.dataset.removeRecipe!); renderHomesteadLab(category) }))
  document.querySelectorAll<HTMLButtonElement>("[data-remove-tutorial]").forEach(button => button.addEventListener("click", () => { removeHomesteadTutorial(button.dataset.removeTutorial!); renderHomesteadLab(category) }))
}
