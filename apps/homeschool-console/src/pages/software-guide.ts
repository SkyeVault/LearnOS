import { getApp, goBack } from "../nav"

export function renderSoftwareGuide() {
  getApp().innerHTML = `<main class="parent-dashboard software-guide"><header><button class="back-btn" id="guide-back">← Admin Corner</button><div><p class="os-kicker">ADMIN SOFTWARE GUIDE</p><h1>Maintain LearnOS with confidence</h1><p>This prototype is local-first: records live in this browser on this device until server sync is introduced.</p></div></header><section class="parent-grid"><article class="parent-panel parent-learning-plan"><p class="os-kicker">HOW IT CONNECTS</p><h2>Runtime path</h2><ol><li><code>src/main.ts</code> starts the app and routes the signed-in role.</li><li><code>src/pages/</code> renders screens and wires controls.</li><li><code>src/app/</code> owns core records, sessions, access, and settings.</li><li><code>src/features/</code> owns modules, assignments, calendar, and deck records.</li><li><code>src/content/</code> contains bundled learning material; <code>src/style.css</code> and <code>src/overhaul.css</code> control visuals.</li></ol><p>Use the flow screen → store function → browser storage → rerender. Keep data writes inside a store rather than directly in a page.</p></article><article class="parent-panel"><p class="os-kicker">USER DATABASE</p><h2>Edit people safely</h2><p>Use <strong>User Management</strong> for normal changes; it writes the same local records used by sign-in.</p><ul><li><code>src/app/store.ts</code>: owner settings and learners; key <code>learning-world-os:data:v1</code>.</li><li><code>src/app/parent-controls.ts</code>: parents, guardians, teachers, relationships, classes, and rosters; key <code>learning-world-os:parent-controls:v1</code>.</li><li><code>src/app/session.ts</code>: current browser session only, not the database.</li></ul><p>For a learner update name, stage, credentials, parent/teacher links, class, and modules in User Management. For an adult update name, role, credentials, and teaching assignments there. Passwords are hashed—never hand-edit a stored password hash.</p></article><article class="parent-panel"><p class="os-kicker">ACCESS</p><h2>Permissions and roles</h2><ul><li><code>src/app/access.ts</code> scopes learner and class visibility.</li><li><code>src/app/parent-controls.ts</code> connects users and groups.</li><li>Admin has global control; teachers are scoped to assigned classes; parents/guardians are scoped to linked learners; learners are scoped to their own workspace.</li></ul><p>Any new sensitive screen should call an access helper before it renders learner or class data.</p></article><article class="parent-panel"><p class="os-kicker">MODULES</p><h2>Create or revise a module</h2><ol><li>Open Module Creator from Admin Corner.</li><li>Choose a template such as Garden Stewards, or a saved module.</li><li>Set ID, name, version, description, and checked home sections.</li><li>Save, then assign it to intended learners.</li></ol><p><code>src/features/modules.ts</code> defines the format. A module is safe metadata—<code>id</code>, <code>name</code>, <code>description</code>, <code>version</code>, and existing <code>subjectNames</code>. It unlocks bundled sections and cannot run downloaded code.</p></article><article class="parent-panel"><p class="os-kicker">MAJOR RECORDS</p><h2>Where to make changes</h2><ul><li>Assignments: <code>src/features/assignments-store.ts</code></li><li>Calendars: <code>src/features/calendar-store.ts</code></li><li>Slide decks: <code>src/features/slide-decks.ts</code> and <code>src/pages/creator-studio-v2.ts</code></li><li>Curriculum plans: <code>src/features/curriculum-planner.ts</code></li><li>Lesson seeds: <code>src/content/</code></li></ul><p>For a new feature, make one focused feature store with a versioned storage key, validation when reading it, and tests for behavior.</p></article><article class="parent-panel"><p class="os-kicker">SAFE MAINTENANCE</p><h2>Before changing code</h2><ol><li>Find the store and its tests before changing data shapes.</li><li>Preserve storage keys or write a migration.</li><li>Use <code>escapeHtml</code> for user-entered strings rendered into HTML.</li><li>Never add plain passwords or child data to source files.</li><li>Run <code>npm run test</code> and <code>npm run build</code> from <code>apps/homeschool-console</code>.</li><li>Test the affected role in a fresh browser session.</li></ol><p>Back up browser profile data before clearing site storage or moving this prototype to another browser or device.</p></article></section></main>`
  document.getElementById("guide-back")!.addEventListener("click", goBack)
}
